# Đổi tên file này thành .env trước khi chạy dự án

# Chuỗi kết nối MongoDB (MongoDB Atlas hoặc local)
MONGODB_URI=mongodb://127.0.0.1:27017/tiemgiatla

# Cổng chạy server
PORT=3000

# Khóa bí mật cho session (nên đổi khi deploy thật)
SESSION_SECRET=tiemgiatla-secret-key-doi-lai-khi-trien-khai
